  
  import React from "react"
  class MyComponent extends React.Component {
    <div>
            <Link to="/">
            <img src={{ siteLogo} } >
            </img>
            </Link>
            <div class="top-menu">
            <MenuLinks links={ menuArr } />
            </div>
        </div>
  
  
    
    handleScroll = e => {
        let element = e.target
        if (element.scrollHeight - element.scrollTop === element.clientHeight) {
        // do something at end of scroll
        }
    }
  }